export const selectUser = (state) => state.userInfo;

export const selectRepos = (state) => state.repos;

export const selectPulls = (state) => state.pulls;

export const selectUserName = (state) => state.userName;

export const selectSelected = (state) => state.selected;
